# The rulings-spec version. Independent of the package version: it moves when a
# recorded meaning changes (MAJOR), a ruling is added (MINOR), or wording is
# clarified without changing meaning (PATCH). Both versions appear in CHANGELOG.md.
version = "0.1.0"
